import { LoadingScreen } from "@/components/LoadingScreen";
export default function Loading() {
  return <LoadingScreen title="Aquí empieza un buen viaje" description="Cargando los packs y las cartas para que lo prepares a tu manera." variant="form" />;
}
