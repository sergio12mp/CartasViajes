import { LoadingScreen } from "@/components/LoadingScreen";
export default function Loading() {
  return <LoadingScreen title="Un momento, capitán" description="Recuperando las cartas y los ajustes de tu viaje." variant="form" />;
}
