import { LoadingScreen } from "@/components/LoadingScreen";
export default function Loading() {
  return <LoadingScreen title="Tu idea puede ser la próxima carta" description="Preparando el espacio para tus propuestas." variant="form" />;
}
