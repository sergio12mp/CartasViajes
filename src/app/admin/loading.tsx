import { LoadingScreen } from "@/components/LoadingScreen";
export default function Loading() {
  return <LoadingScreen title="Cargando el panel" description="Recuperando la información de administración." variant="feed" />;
}
